let EventList = ["https://www.nmit.ac.in"];

// async function linkAddition(){
//   result = await fetch();//Should decide api endpoint
//   for(let i = 0;i<result.length;i++)
//     EventList.push(result[i]);

// }

export default EventList;